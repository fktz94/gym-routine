import { StyleSheet, Text, View } from "react-native";

const EditExerciseModal = () => {
  return (
    <View>
      <Text>EditExerciseModal</Text>
    </View>
  );
};

// JUST STARTED WORKING ON UNIFY ALL EDIT EXERCISE MODAL INTO ONE REUSABLE WITH THE CUSTOM HOOK

export default EditExerciseModal;

const styles = StyleSheet.create({});
